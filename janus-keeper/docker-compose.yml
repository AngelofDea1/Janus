version: "3.8"

services:
  keeper:
    build: .
    container_name: janus-keeper
    restart: always
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
